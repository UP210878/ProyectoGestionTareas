import React from 'react';
import { Button } from '@mui/material';

const AddActivity = ({ activity }) => {

    const addMoreActivity = () => {
        
    }

  return (
    <div>
        <Button variant="outlined" onClick={addMoreActivity}>Add Activity</Button>
    </div>
  );
};

export default AddActivity;